# This workflow is used to build the Next.js application.
name: Next.js CI/CD

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"
      
      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build --if-present
        env:
          NEXT_PUBLIC_FIREBASE_API_KEY: ${{ secrets.AIzaSyBFaEaUs7Hv5CgdBuVt9tC4iuHBI6skGVo}}
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${{ secrets.ruralstar.firebaseapp.com}}
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${{ secrets.ruralstar}}
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: ${{ secrets.ruralstar.firebasestorage.app}}
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.445949941044}}
          NEXT_PUBLIC_FIREBASE_APP_ID: ${{ secrets.1:445949941044:web:7d3169955b2853c93a8161}}
          NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

